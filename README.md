# Periodic Table Lookup

Small project with a script to lookup element data from `atomic_mass.txt`.

Usage:

```bash
# From project root
bash periodic_table/element.sh H
bash periodic_table/element.sh 1
bash periodic_table/element.sh Hydrogen
```

Run tests:

```bash
bash tests/test_element.sh
```
